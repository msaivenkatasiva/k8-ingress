kind: Deployment
apiVersion: apps/v1
metadata:
  name: app1
  # namespace: expense
  labels:
    app: app1
    purpose: ingress
spec:
  replicas: 5
  selector:
    matchLabels:
      app: app1
      purpose: ingress
  template:
    metadata:
      labels:
        app: app1
        purpose: ingress
    spec:
      containers:
      - name: app1
        image: msaivenkatasiva/app1:1.0
---
apiVersion: v1
kind: Service
metadata:
  name: app1
  # namespace: expense
spec:
  selector:
    app: app1
    purpose: ingress
  ports:
  - protocol: TCP
    port: 80 #service-port
    targetPort: 80 #target-port